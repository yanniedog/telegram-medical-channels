# Python
__pycache__/
*.py[cod]
*$py.class
.Python
.venv/
venv/
env/
*.egg-info/
dist/
build/
.pytest_cache/

# Secrets and local env
.env
.env.*

# Logs and scratch (repo root and subdirs)
*.log
hits.txt
verify*.txt
inspect*.txt
*_out.txt
expand_out.txt
probe_*.txt
telemetr_test*.txt
tgstat_batch.txt
tme_case.txt
test_subs_out.txt
parse_au_out.txt
build_swarm_out.txt
_tmp_*.py
_tmp_*.json

# Generated workbooks (rebuild: python build_from_cache.py)
*.xlsx
*.xls

# HTML sample / probe dumps
*.html

# Scraped post cache (large; keep directory via .gitkeep)
data/posts/*
!data/posts/.gitkeep

# Regenerated registry, discovery, and swarm runtime JSON under data/
data/channels_registry.json
data/rescrape_queue.json
data/discovered_channels.json
data/agent_discoveries_merged.json
data/agent_swarm_merged.json
data/swarm_tasks.json
data/swarm_checkpoint.json
data/agent_*.json

# Swarm shard and per-wave outputs (keep directory via .gitkeep)
data/agent_swarm/*
!data/agent_swarm/.gitkeep

# Discovery seeds at repo root (rebuilt by merge / discovery scripts)
channel_seeds.json

# Ephemeral swarm scratch at repo root
swarm_au_out.json

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*~

# Chief hook dedupe state (local only)
.cursor/chief-session-state.json
